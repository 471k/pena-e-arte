apiVersion: apps/v1
kind: DaemonSet
metadata:
  name: tattooos-alloy
  namespace: monitoring
spec:
  selector:
    matchLabels:
      app: tattooos-alloy
  template:
    metadata:
      labels:
        app: tattooos-alloy
    spec:
      serviceAccountName: tattooos-alloy
      containers:
        - name: alloy
          image: grafana/alloy:v1.18.0
          args:
            - run
            - /etc/alloy/config.alloy
            - --server.http.listen-addr=0.0.0.0:12345
          env:
            - name: NODE_NAME
              valueFrom:
                fieldRef:
                  fieldPath: spec.nodeName
          ports:
            - containerPort: 12345
          volumeMounts:
            - name: config
              mountPath: /etc/alloy/config.alloy
              subPath: config.alloy
            # Node-local, kubelet-standardized log path — read-only, no container-runtime
            # socket needed (see alloy-configmap.yaml's comment).
            - name: varlogpods
              mountPath: /var/log/pods
              readOnly: true
          resources:
            requests:
              cpu: 50m
              memory: 128Mi
            limits:
              cpu: 250m
              memory: 256Mi
      volumes:
        - name: config
          configMap:
            name: tattooos-alloy-config
        - name: varlogpods
          hostPath:
            path: /var/log/pods
---
apiVersion: v1
kind: ServiceAccount
metadata:
  name: tattooos-alloy
  namespace: monitoring
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: tattooos-alloy
rules:
  - apiGroups: [""]
    resources: ["pods", "nodes"]
    verbs: ["get", "list", "watch"]
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: tattooos-alloy
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: ClusterRole
  name: tattooos-alloy
subjects:
  - kind: ServiceAccount
    name: tattooos-alloy
    namespace: monitoring
