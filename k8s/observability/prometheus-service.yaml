apiVersion: v1
kind: Service
metadata:
  name: pena-e-arte-prometheus
  namespace: monitoring
spec:
  type: ClusterIP
  selector:
    app: pena-e-arte-prometheus
  ports:
    - port: 9090
      targetPort: 9090
