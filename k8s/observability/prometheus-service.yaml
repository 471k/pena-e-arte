apiVersion: v1
kind: Service
metadata:
  name: tattooos-prometheus
  namespace: monitoring
spec:
  type: ClusterIP
  selector:
    app: tattooos-prometheus
  ports:
    - port: 9090
      targetPort: 9090
